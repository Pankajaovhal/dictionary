# d={"apple":2,"ball":{"b":1,"c":4},"dog":6,"gun":9,"hat":3,"hat":4}
# # b=d.fromkeys(d)
# del d
# # d.popitem()      #removes recent values(last value)
# # d.pop("apple")      #remove element if we put any key as an argument
# # d.clear()         #removes all element from dict
# # d.copy()            #copy thi dict as it is 
# # d.fromkeys()
# # b=d.get("gun")
# # b=d.items()        #returns all items in list 
# # b=d.keys()           #returns all keys in list
# # b=d.values()          #returns all values from dict in list
# # b=d.setdefault("y",6)     #returns values of key and if the key don't exist it inserts that key in dict
# # b=d.update({"u":5})       #insertes key n values pair in the end of the dict
# # print(b)


a=[2,3,4,5,6]
b=a.append(a)
print(b)